		Flet app interface for ds.upatras.gr
		

		In order to launch the app:
		
			make sure to activate the virtual emviroment called .venv
			if nessesary install all modules in requirments.txt
				using pip install requirments.txt
			lauch the mainX.X.py app were X.X is the current version
				make sure that u are using the latest version of main.py file
			files make_a_request.py and trandlate.py are containing essensial functions 
			for the app so make sure are in the same directory with mainX.X.py

	if the files have some problem install them through the repo:
	